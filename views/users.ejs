<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>create</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-screen pt-5 leading-10 font-sans bg-zinc-900">
      <a
        href="/"
        class="text-blue-700 font-bold px-10 text-2xl hover:text-blue-800"
        >let's create another user</a
      >
      <div class="task p-10 flex flex-wrap flex-row gap-3">
        <% if (allUsers.length > 0) { %> <% allUsers.forEach(function(val){ %>
        <div class="w-80 text-nowrap h-max bg-zinc-800 p-2 rounded-md">
          <h1 class="text-white leading-7 font-semibold text-2xl">
            <p class="text-zinc-400 text-xl">Name :</p>
            <%= val.name %>
          </h1>
          <h1 class="text-white font-semibold text-2xl text-xl">
            <p class="text-zinc-400 text-xl">Mob_No :</p>
            <%= val.mobileNumber %>
          </h1>
          <h1 class="text-white font-semibold text-2xl text-xl">
            <p class="text-zinc-400 text-xl">Age :</p>
            <%= val.age %>
          </h1>
          <h1 class="text-white font-semibold text-2xl text-xl">
            <p class="text-zinc-400 text-xl">Gender :</p>
            <%= val.gender %>
          </h1>
          <div class="flex justify-evenly">
            <form
              action="/delete/<%= val._id %>"
              method="POST"
              class="inline-block mt-4"
            >
              <input type="hidden" name="_method" value="DELETE" />
              <button
                type="submit"
                class="text-red-500 text-xl font-semibold hover:text-red-400 transition-transform duration-400 hover:scale-110 inline-block px-8 font-semibold py-1 rounded-lg"
              >
                Delete
              </button>
            </form>
            <!-- Edit button -->
            <a href="/edit/<%= val._id %>">
              <button
                type="submit"
                class="text-blue-600 transition-transform duration-400 hover:scale-110 text-xl my-2 mt-3 inline-block px-8 font-semibold py-1 rounded-lg"
              >
                Edit
              </button>
            </a>
          </div>
        </div>
        <% }) %> <% } else { %>
        <div class="pr-10">
          <h3 class="text-white">No Users yet :)</h3>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
