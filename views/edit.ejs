<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Update User</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-screen justify-between bg-zinc-900">
      <div></div>
      <div class="w-full">
        <h1 class="text-white font-bold px-10 text-4xl pt-10">
          Upadate the user
        </h1>
        <form
          method="post"
          class="flex-col flex gap-10 pl-5 pt-10 w-5/12"
          action="/update/<%= user._id %>"
        >
          <input
            class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 flex-col text-white outline-none"
            type="text"
            name="name"
            placeholder="name"
            value="<%= user.name %>"
          />
          <input
            class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 flex-col text-white outline-none"
            type="text"
            name="gender"
            placeholder="Gender"
            value="<%= user.gender %>"
          />

          <input
            class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 flex-col text-white outline-none"
            type="Number"
            name="mobileNumber"
            placeholder="mobileNumber"
            value="<%= user.mobileNumber %>"
          />
          <input
            class="px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 flex-col text-white outline-none"
            type="number"
            name="age"
            placeholder="age"
            value="<%= user.age %>"
          />
          <input
            class="bg-blue-700 px-5 py-2 rounded-md text-2xl font-bold text-white cursor-pointer hover:bg-blue-900"
            type="submit"
            value="Edit"
          />
        </form>
      </div>
    </div>
  </body>
</html>
